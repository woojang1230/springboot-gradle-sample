# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: Java CI with Gradle

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: env-test
      env:
        USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
      run: echo "$env:USERNAME"
    - run: echo ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
    - name: Check out the repo
      uses: actions/checkout@v2
        
    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: woojang1230
        password: abe886db-75e1-4e3c-9d42-58bc70b2d9ac
    
#    - name: Set up JDK 11
#      uses: actions/setup-java@v2
#      with:
#        java-version: '11'
#        distribution: 'adopt'
#        cache: gradle
    
#    - name: Grant execute permission for gradlew
#      run: chmod +x gradlew
#    - name: Build with Gradle
#      run: ./gradlew clean build
    
#    - name: Extract metadata (tags, labels) for Docker
#      id: meta
#      uses: crazy-max/ghaction-docker-meta@v1
#      with:
#        images: woojang1230/push-test
#        tags: 2.0

#    - name: Build and push Docker image
#      uses: docker/build-push-action@v2
#      with:
#        context: .
#        file: ./Dockerfile
#        platforms: linux/amd64
#        push: true
#        tags: ${{ steps.meta.with.tags }}
#        labels: ${{ steps.meta.with.images }}
